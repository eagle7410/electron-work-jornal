/**
 * Constants for names models.
 * @type {{cat: string, usr: string, sett: string, store: string}}
 */
module.exports = {
	prj   : 'projects',
	usr   : 'users',
	sett  : 'settings',
	store : 'storage'
};
